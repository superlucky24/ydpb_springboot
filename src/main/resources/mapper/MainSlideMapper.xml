<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.ydpb.mapper.MainSlideMapper">

    <select id="getList" resultType="kr.go.ydpb.domain.MainSlideVO">
        SELECT
            slide_id AS slideId,
            title,
            image_path AS imagePath,
            link_url AS linkUrl,
            use_yn AS useYn,
            sort_order AS sortOrder,
            reg_date AS regDate
        FROM tbl_main_slide
        ORDER BY sort_order ASC
    </select>

    <select id="get" resultType="kr.go.ydpb.domain.MainSlideVO">
        SELECT
            slide_id AS slideId,
            title,
            image_path AS imagePath,
            link_url AS linkUrl,
            use_yn AS useYn,
            sort_order AS sortOrder,
            reg_date AS regDate
        FROM tbl_main_slide
        WHERE slide_id = #{slideId}
    </select>

    <insert id="insert">
        INSERT INTO tbl_main_slide (
            slide_id, title, image_path, link_url, sort_order, use_yn
        ) VALUES (
                     seq_main_slide.nextval,
                     #{title},
                     #{imagePath},
                     #{linkUrl},
                     #{sortOrder},
                     #{useYn}
                 )
    </insert>

    <update id="update">
        UPDATE tbl_main_slide
        SET title = #{title},
            image_path = #{imagePath},
            link_url = #{linkUrl},
            sort_order = #{sortOrder},
            use_yn = #{useYn}
        WHERE slide_id = #{slideId}
    </update>

    <delete id="delete">
        DELETE FROM tbl_main_slide WHERE slide_id = #{slideId}
    </delete>

</mapper>