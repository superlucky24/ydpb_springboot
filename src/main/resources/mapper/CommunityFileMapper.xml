<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.ydpb.mapper.CommunityFileMapper">

    <insert id="insert">
        <selectKey keyProperty="fileId" order="BEFORE" resultType="long">
            select seq_community_file.nextval from dual
        </selectKey>
        insert into tbl_community_file(
        file_id, cmnt_id, uuid, file_name, upload_path, alt_text, insert_yn
        ) values (
        #{fileId}, #{cmntId}, #{uuid}, #{fileName}, #{uploadPath}, #{altText}, #{insertYn}
        )
    </insert>


    <select id="read" resultType="kr.go.ydpb.domain.CommunityFileVO">
        select * from tbl_community_file where file_id = #{fileId}
    </select>

    <select id="getFilesByCmntId" resultType="kr.go.ydpb.domain.CommunityFileVO">
        select * from tbl_community_file where cmnt_id = #{cmntId}
    </select>

    <delete id="deleteFileByCmntId">
        delete from tbl_community_file where cmnt_id = #{cmntId}
    </delete>

    <delete id="deleteFileByFileId">
        delete from tbl_community_file where file_id = #{fileId}
    </delete>


</mapper>
